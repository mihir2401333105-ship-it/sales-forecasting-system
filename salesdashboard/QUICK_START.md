# Quick Start Guide

### Step 1: Install Dependencies
```bash
npm install
```

### Step 2: Configure API
The dashboard currently connects to `http://localhost:8000/predict`.
Make sure your FastAPI server is running.

### Step 3: Run the Dashboard
```bash
npm run dev
```
Open `http://localhost:5173` to see the dashboard in action!
